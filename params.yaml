prepare:
  image_directory: 'data_first_draft'
  annotations_directory: 'annotattions-first-train-draft'
  train_proportion: 0.7
  validation_proportion: 0.2
  test_proportion: 0.1
  seed: 4224
  gamma: 0.4
  adjust_images_brightness_strategy: 'None'
  percentile: 25

train:
  image_size: 640
  epochs: 250
  batch_size: 16
  init_lr: 0.0001
  patience: 15
  seed: 4224
  optimizer: 'Adam'
  directory_output: 'output'

test:
  directory_output: 'output'
